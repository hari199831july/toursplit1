const KEY='toursplit-data-v1';
const money=n=>`₹${Number(n||0).toLocaleString('en-IN',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const uid=()=>Date.now().toString(36)+Math.random().toString(36).slice(2,7);
let state=JSON.parse(localStorage.getItem(KEY)||'null')||{tour:null,members:[],expenses:[]}; let editing={type:null,id:null}; const $=s=>document.querySelector(s); const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])); const initials=n=>(n||'?').split(' ').map(x=>x[0]).join('').slice(0,2).toUpperCase();
function calculations(){const total=state.expenses.reduce((a,e)=>a+Number(e.amount||0),0), share=state.members.length?total/state.members.length:0; const paid={};state.members.forEach(m=>paid[m.id]=0);state.expenses.forEach(e=>{if(paid[e.paidBy]!==undefined)paid[e.paidBy]+=Number(e.amount||0)});return {total,share,paid,balances:state.members.map(m=>({...m,paid:paid[m.id]||0,balance:(paid[m.id]||0)-share}))};}
function settlement(balances){let creditors=balances.filter(x=>x.balance>.005).map(x=>({...x,amount:x.balance})).sort((a,b)=>b.amount-a.amount), debtors=balances.filter(x=>x.balance<-.005).map(x=>({...x,amount:-x.balance})).sort((a,b)=>b.amount-a.amount), out=[];let i=0,j=0;while(i<debtors.length&&j<creditors.length){const amount=Math.min(debtors[i].amount,creditors[j].amount);out.push({from:debtors[i].name,to:creditors[j].name,amount});debtors[i].amount-=amount;creditors[j].amount-=amount;if(debtors[i].amount<.005)i++;if(creditors[j].amount<.005)j++;}return out;}
function render(){const active=!!state.tour;$('#emptyState').hidden=active;$('#dashboard').hidden=!active;const c=calculations();$('#totalSpent').textContent=money(c.total);$('#memberCount').textContent=state.members.length;$('#expenseCount').textContent=state.expenses.length;$('#averageSpent').textContent=money(c.share);if(!active)return;$('#tourTitle').textContent=state.tour.name;$('#tourMeta').textContent=`${state.tour.start||'No start date'}${state.tour.end?' → '+state.tour.end:''} · ${state.members.length} members`;const settled=c.balances.filter(x=>Math.abs(x.balance)<.01).length;$('#settledCount').textContent=`${settled} settled`;
$('#balanceList').innerHTML=c.balances.length?c.balances.map(m=>{const positive=m.balance>.005;return `<div class="balance-row"><span class="avatar">${initials(m.name)}</span><div class="row-main"><strong>${esc(m.name)}</strong><small>Paid ${money(m.paid)} · Share ${money(c.share)}</small></div><strong class="${positive?'positive':m.balance<-.005?'negative':'neutral'}">${positive?'Gets back ':m.balance<-.005?'Owes ':'Settled '}${positive||m.balance<-.005?money(Math.abs(m.balance)):''}</strong></div>`}).join(''):'<p class="muted empty-line">Add members to see balances.</p>';
const transfers=settlement(c.balances);$('#settlementTable').innerHTML=transfers.length?`<div class="settlement-head"><span>Member paying</span><span>Member receiving</span><span>Amount</span></div>`+transfers.map(t=>`<div class="settlement-row"><span><b>${esc(t.from)}</b><small>owes</small></span><span><b>${esc(t.to)}</b><small>receives</small></span><strong>${money(t.amount)}</strong></div>`).join(''):'<div class="all-settled"><span>✓</span><div><strong>Everyone is settled</strong><p>No payments are needed right now.</p></div></div>';
$('#expenseList').innerHTML=state.expenses.length?`<div class="expense-header"><span>Description</span><span>Paid by</span><span>Amount</span><span></span></div>`+state.expenses.map(e=>{const p=state.members.find(m=>m.id===e.paidBy);return `<div class="expense-row"><span><strong>${esc(e.description)}</strong><small>${e.date||''}</small></span><span>${esc(p?.name||'Unknown')}</span><strong>${money(e.amount)}</strong><span class="row-actions"><button onclick="editExpense('${e.id}')">Edit</button><button onclick="deleteExpense('${e.id}')">Delete</button></span></div>`}).join(''):'<p class="muted empty-line">No expenses added yet.</p>';
$('#memberList').innerHTML=state.members.length?state.members.map(m=>`<div class="member-row"><span class="avatar">${initials(m.name)}</span><div class="row-main"><strong>${esc(m.name)}</strong><small>${esc(m.contact||'')}</small></div><span class="row-actions"><button onclick="editMember('${m.id}')">Edit</button><button onclick="deleteMember('${m.id}')">Delete</button></span></div>`).join(''):'<p class="muted empty-line">No members added yet.</p>';}
const save=()=>{localStorage.setItem(KEY,JSON.stringify(state));render()};function openModal(title,html){$('#modalTitle').textContent=title;$('#modalForm').innerHTML=html+`<div class="form-actions"><button type="button" class="button secondary" id="cancelModal">Cancel</button><button class="button button-primary" type="submit">Save</button></div>`;$('#modal').hidden=false;setTimeout(()=>$('#modalForm input')?.focus(),30)}function closeModal(){$('#modal').hidden=true;editing={type:null,id:null}}
const tourForm=t=><div class="field"><label>Tour name *</label><input name="name" required value="${esc(t.name)}" placeholder="e.g. Kerala trip 2026"></div><div class="form-grid"><div class="field"><label>Start date</label><input name="start" type="date" value="${esc(t.start)}"></div><div class="field"><label>End date</label><input name="end" type="date" value="${esc(t.end)}"></div></div>`;const memberForm=m=><div class="field"><label>Name *</label><input name="name" required value="${esc(m.name)}" placeholder="e.g. Rahul"></div><div class="field"><label>Email or phone</label><input name="contact" value="${esc(m.contact)}"></div>`;const expenseForm=e=>`<div class="field"><label>Description *</label><input name="description" required value="${esc(e.description)}" placeholder="e.g. Hotel booking"></div><div class="form-grid"><div class="field"><label>Amount *</label><input name="amount" required type="number" min="0.01" step="0.01" value="${esc(e.amount)}"></div><div class="field"><label>Paid by *</label><select name="paidBy" required>${state.members.map(m=>`<option value="${m.id}" ${m.id===e.paidBy?'selected':''}>${esc(m.name)}</option>`).join('')}</select></div></div><div class="field"><label>Date</label><input name="date" type="date" value="${esc(e.date||new Date().toISOString().slice(0,10))}"></div>`;
function showToast(t){const x=$('#toast');x.textContent=t;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2200)}
function editMember(id){editing={type:'member',id};openModal('Edit member',memberForm(state.members.find(x=>x.id===id)||{}))}function deleteMember(id){if(confirm('Delete this member? Expenses paid by them will remain as Unknown.')){state.members=state.members.filter(m=>m.id!==id);state.expenses.forEach(e=>{if(e.paidBy===id)e.paidBy='unknown'});save();showToast('Member deleted')}}function editExpense(id){editing={type:'expense',id};openModal('Edit expense',expenseForm(state.expenses.find(x=>x.id===id)||{}))}function deleteExpense(id){if(confirm('Delete this expense?')){state.expenses=state.expenses.filter(e=>e.id!==id);save();showToast('Expense deleted')}}
$('#newTourBtn').onclick=$('#emptyNewTour').onclick=()=>{editing={type:'tour'};openModal(state.tour?'Edit tour':'Create a tour',tourForm(state.tour||{}))};$('#editTourBtn').onclick=()=>{editing={type:'tour'};openModal('Edit tour',tourForm(state.tour))};$('#deleteTourBtn').onclick=()=>{if(confirm('Delete this tour and all its members and expenses?')){state={tour:null,members:[],expenses:[]};save();showToast('Tour deleted')}};$('#addMemberBtn').onclick=()=>{editing={type:'member'};openModal('Add member',memberForm())};$('#addExpenseBtn').onclick=()=>{if(!state.members.length)return showToast('Add a member first');editing={type:'expense'};openModal('Add expense',expenseForm({}))};$('#closeModal').onclick=closeModal;$('#modal').onclick=e=>{if(e.target.id==='modal')closeModal()};document.addEventListener('click',e=>{if(e.target.id==='cancelModal')closeModal()});$('#modalForm').onsubmit=e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));if(editing.type==='tour')state.tour={...(state.tour||{}),...d};if(editing.type==='member'){if(editing.id)state.members=state.members.map(m=>m.id===editing.id?{...m,...d}:m);else state.members.push({id:uid(),...d})}if(editing.type==='expense'){d.amount=Number(d.amount);if(editing.id)state.expenses=state.expenses.map(x=>x.id===editing.id?{...x,...d}:x);else state.expenses.push({id:uid(),...d})}save();closeModal();showToast('Saved successfully')};document.querySelectorAll('.tab').forEach(tab=>tab.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));tab.classList.add('active');$('#expensesTab').hidden=tab.dataset.tab!=='expenses';$('#membersTab').hidden=tab.dataset.tab!=='members'});$('#printBtn').onclick=()=>window.print();render();
